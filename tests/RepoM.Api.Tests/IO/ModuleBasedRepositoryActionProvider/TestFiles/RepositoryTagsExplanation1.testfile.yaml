# begin-snippet: RepositoryActionsV1RepositoryTagsYaml

# array of objects.
repository-tags:
  # variable section for tags (same structure as global variable section)
  variables:

  tags:

    # 'tag' is the name of the tag (required, string, no-spaces).
  - tag: ''
  
    # 'when' is a predicate when to assign the tag to a repository (optional, string/boolean, evaluated, default true).
    when: ''

  - tag: ''
    when: ''
  - tag: ''
    when: ''

  # end-snippet
