<UserControl x:Class="RepoM.App.Controls.RepositoryComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:RepoM.App.Controls"
             xmlns:git="clr-namespace:RepoM.Api.Git;assembly=RepoM.Api"
             d:DataContext="{d:DesignInstance git:RepositoryViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">
    <StackPanel Background="Transparent"
                MouseEnter="Root_MouseEnter"
                MouseLeave="Root_MouseLeave">
        <DockPanel>
            <TextBlock DockPanel.Dock="Right"
                       Margin="4,0,8,3"
                       TextElement.FontSize="20"
                       VerticalAlignment="Center"
                       Cursor="Hand"
                       ToolTip="Toggle favorite"
                       MouseLeftButtonUp="FavoriteToggle_Click">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="&#x2605;" />
                        <Setter Property="Foreground" Value="Gray" />
                        <Setter Property="Opacity" Value="0.2" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                <Setter Property="Foreground" Value="#FFD700" />
                                <Setter Property="Opacity" Value="1.0" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <TextBlock DockPanel.Dock="Right"
                       Margin="4,3,8,3"
                       TextElement.FontSize="14"
                       Cursor="Hand"
                       ToolTip="Toggle active monitoring"
                       MouseLeftButtonUp="MonitoringToggle_Click">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="&#x1F441;" />
                        <Setter Property="Foreground" Value="Gray" />
                        <Setter Property="Opacity" Value="0.3" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMonitored}" Value="True">
                                <Setter Property="Foreground" Value="#FF42A5F5" />
                                <Setter Property="Opacity" Value="1.0" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}"
                           ToolTip="{Binding Path}"
                           TextElement.FontSize="17"
                           Foreground="{DynamicResource AccentColorBrush}"
                           Margin="8,3,8,3" />
            </StackPanel>
        </DockPanel>

        <TextBlock Text="{Binding CurrentBranch}"
                   FontSize="13.5"
                   Margin="8,3,8,3"
                   Visibility="{Binding Path=IsNotBare, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <StackPanel Orientation="Horizontal">
            <Border Margin="8,5,8,3"
                    Padding="6,2,6,2"
                    BorderThickness="0"
                    Background="#14FFFFFF"
                    CornerRadius="5"
                    HorizontalAlignment="Left">
                <TextBlock Text="{Binding Status}"
                           FontFamily="Consolas"
                           FontSize="13.5" />
            </Border>

            <StackPanel Orientation="Horizontal">
                <ItemsControl ItemsSource="{Binding Tags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="tagBorder"
                                    Margin="8,5,8,3"
                                    Padding="6,2,6,2"
                                    BorderThickness="0"
                                    Background="#14FFFFFF"
                                    CornerRadius="5"
                                    HorizontalAlignment="Left"
                                    Cursor="Hand"
                                    d:DataContext="{d:DesignInstance git:TagViewModel, IsDesignTimeCreatable=False}"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.InputBindings>
                                    <MouseBinding Gesture="LeftClick"
                                                  Command="{Binding DataContext.AddQuickFilterTagCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:RepositoryList}}}"
                                                  CommandParameter="{Binding Tag}" />
                                </Border.InputBindings>
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                To="#40FFFFFF"
                                                                Duration="0:0:0.15">
                                                    <ColorAnimation.EasingFunction>
                                                        <QuadraticEase EasingMode="EaseOut" />
                                                    </ColorAnimation.EasingFunction>
                                                </ColorAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                                 To="1.05"
                                                                 Duration="0:0:0.1">
                                                    <DoubleAnimation.EasingFunction>
                                                        <QuadraticEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                                 To="1.05"
                                                                 Duration="0:0:0.1">
                                                    <DoubleAnimation.EasingFunction>
                                                        <QuadraticEase EasingMode="EaseOut" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                To="#14FFFFFF"
                                                                Duration="0:0:0.2">
                                                    <ColorAnimation.EasingFunction>
                                                        <QuadraticEase EasingMode="EaseIn" />
                                                    </ColorAnimation.EasingFunction>
                                                </ColorAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                                                 To="1.0"
                                                                 Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <QuadraticEase EasingMode="EaseIn" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                                 To="1.0"
                                                                 Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <QuadraticEase EasingMode="EaseIn" />
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <TextBlock x:Name="tagText"
                                           Text="{Binding Tag}"
                                           FontFamily="Consolas"
                                           Foreground="#FFED8888"
                                           FontSize="13.5">
                                    <TextBlock.Triggers>
                                        <EventTrigger RoutedEvent="MouseEnter">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)"
                                                                    To="#FFFFFFFF"
                                                                    Duration="0:0:0.15">
                                                        <ColorAnimation.EasingFunction>
                                                            <QuadraticEase EasingMode="EaseOut" />
                                                        </ColorAnimation.EasingFunction>
                                                    </ColorAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                        <EventTrigger RoutedEvent="MouseLeave">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)"
                                                                    To="#FFED8888"
                                                                    Duration="0:0:0.2">
                                                        <ColorAnimation.EasingFunction>
                                                            <QuadraticEase EasingMode="EaseIn" />
                                                        </ColorAnimation.EasingFunction>
                                                    </ColorAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </TextBlock.Triggers>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>